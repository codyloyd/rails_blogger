<h1>All Tags</h1>

<ul id="articles">
  <% @tags.each do |tag| %>
    <li>
      <%= link_to tag.name, tag_path(tag) %> <%= link_to "(Delete this Tag)", tag_path(tag), method: :delete, data: {confirm: "Really delete this tag?"} %>
    </li>
  
  <% end %>
  
</ul>